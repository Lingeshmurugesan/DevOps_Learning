      --Visual Studio Terminal commends--

git clone 'repo url' = to clone git repository to local machine
git status = to chek status of repository
git checkout -b 'branch name' = to create new branch
git checkout 'branch name' = to change branch
git add . = to save the code changes to staging area before commit
git push origin 'branch name' = push changes to git repo
git pull origin 'branch name' = pull changes to git repo
git merge 'branch name' = merge a branch into current branch


      --ec2 instance linux cmd--
ec2-user - login as 
cd /var/www/html = switch to root user(git)
sudo git pull origin 'branch name' = depoly code to production


      --git cmd--
git init = to inialize git repository
git add = after changes of code need to perform git add to add the changes line to the old code
git commit = to save the changes with following -m (message)
  eg: git commit -m updated version v1.1
git push = used to push our entier code to git repository
git diff = will show exact changes modified in github
git log = check the versions with unique no.
git reset --hard 'unique no. of any version' = to change / switch version

